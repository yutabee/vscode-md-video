node_modules/
out/
out-test/
dist/
*.vsix
.DS_Store

# Local secrets — never commit (holds VSCE_PAT / OVSX_PAT etc.)
.env
.env.*

# Extracted audio cache (created in user workspaces, not this repo)
.vscode-md-video-cache/

# Playwright MCP session artifacts (local browser-automation logs/screenshots)
.playwright-mcp/
signin-state.png
